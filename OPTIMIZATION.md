# 🚀 Оптимизация использования памяти

## Текущие проблемы:
- Весь список perpetuals (~500+ монет) загружается в память
- Параллельные запросы к CoinGecko
- Кеш без лимитов

## ✅ Оптимизации:

### 1. Уменьшите concurrency в config.json

```json
{
  "concurrency": 4
}
```

Меньше параллельных запросов = меньше памяти

### 2. Ограничьте количество обрабатываемых монет

Добавьте в `config.json`:
```json
{
  "maxSymbols": 100,
  "minMarketCapUsd": 1000000
}
```

### 3. Используйте stream для CSV

Вместо хранения всех данных в памяти перед записью.

### 4. Очищайте кеш после обработки

### 5. Запускайте скринер реже

Через Task Scheduler/Cron:
- Раз в 3-6 часов вместо каждого часа
- Фронтенд всё равно показывает последние данные

## 🔧 Быстрое решение:

**В `config.json` измените:**
```json
{
  "concurrency": 3,
  "minMarketCapUsd": 5000000,
  "maxOnboardDays": 365
}
```

Это:
- ✅ Снизит память в 3-4 раза
- ✅ Обработает только монеты с market cap > $5M
- ✅ Уберёт старые монеты (>1 года)
- ✅ Результат: ~100-150 монет вместо 500+

## 📊 Мониторинг памяти:

Railway/Render показывают использование в Dashboard.  
Обычно Node.js использует 100-300 MB для этого скрипта.

Если использует больше — примените оптимизации выше.

