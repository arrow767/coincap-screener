# 🚀 Binance Perpetual Screener

Скринер перпетуальных контрактов Binance с данными CoinGecko и веб-интерфейсом для анализа.

## Возможности

- ✅ Парсинг всех USDT-M перпетуалов с Binance
- 📊 Получение данных с CoinGecko (market cap, FDV, onboard date, контракты)
- 💹 Отображение 24h изменений цены
- 🔍 Фильтры по числовым значениям и наличию спота
- ⭐ Избранные монеты с сохранением в localStorage
- 🆕 Подсветка новых токенов (<10 дней)
- 📋 Копирование адресов контрактов
- 🔗 Ссылки на CoinGecko, CryptoRank, CoinMarketCap
- 🔄 Автообновление данных каждые 60 секунд

## Установка

```bash
# Корневая папка
npm install

# Фронтенд
cd frontend
npm install
cd ..
```

## Конфигурация

Скопируйте `config.example.json` в `config.json` и настройте параметры:

```json
{
  "concurrency": 12,
  "progressIntervalMs": 3000,
  "priceDiffPctMax": 20,
  "minMarketCapUsd": null,
  "maxMarketCapUsd": null,
  "minFdvUsd": null,
  "maxFdvUsd": null,
  "minOnboardDays": null,
  "maxOnboardDays": null
}
```

### Прокси (опционально)

Создайте `proxy.txt` со списком прокси (по одному на строку):

```
http://user:pass@host:port
socks5://host:port
```

## Разработка

### Запуск в режиме разработки

```bash
# Бэкенд + фронтенд одновременно
npm run dev:all
```

- Бэкенд: запускает скринер, генерирует CSV в `output/`
- Фронтенд: http://localhost:3000 с hot reload

### Только бэкенд

```bash
npm run start
```

### Только фронтенд

```bash
cd frontend
npm run dev
```

## Production деплой

### 1. Билд проекта

```bash
npm run build:all
```

Это:
- Создаёт `dist/` с скомпилированным TypeScript
- Билдит фронтенд в `output/dist/`

### 2. Запуск в production

```bash
# Сначала запустите скринер для генерации CSV
npm run start:prod

# В отдельном терминале запустите веб-сервер
npm run start:server
```

Или всё вместе:

```bash
npm run deploy
```

Приложение будет доступно на http://localhost:8080

### 3. Автозапуск скринера

Настройте cron/планировщик для периодического запуска:

**Linux/Mac (cron):**
```bash
# Каждый час
0 * * * * cd /path/to/coincap_screener && npm run start:prod
```

**Windows (Task Scheduler):**
1. Создайте задачу с триггером (например, каждый час)
2. Действие: запустите `node dist/index.js` в папке проекта

### 4. Деплой на сервер

#### Вариант A: PM2 (рекомендуется)

```bash
npm install -g pm2

# Скринер (запускается раз в час)
pm2 start dist/index.js --name screener --cron "0 * * * *" --no-autorestart

# Веб-сервер
pm2 start dist/server.js --name web

pm2 save
pm2 startup
```

#### Вариант B: Docker

Создайте `Dockerfile`:

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --production
COPY . .
RUN npm run build:all
EXPOSE 8080
CMD ["sh", "-c", "node dist/index.js && node dist/server.js"]
```

```bash
docker build -t coincap-screener .
docker run -d -p 8080:8080 coincap-screener
```

#### Вариант C: VPS/Dedicated

1. Установите Node.js 18+
2. Клонируйте репозиторий
3. `npm install && cd frontend && npm install && cd ..`
4. `npm run build:all`
5. Настройте systemd/supervisor для автозапуска

## Структура проекта

```
├── src/
│   ├── index.ts          # Основной скринер
│   ├── server.ts         # Express сервер для production
│   ├── binance.ts        # API Binance
│   ├── coingecko.ts      # API CoinGecko
│   ├── config.ts         # Конфигурация
│   ├── match.ts          # Утилиты
│   └── utils/http.ts     # HTTP клиент с retry
├── frontend/
│   ├── src/
│   │   ├── App.tsx       # Основной компонент
│   │   ├── App.css       # Стили
│   │   ├── types.ts      # TypeScript типы
│   │   └── main.tsx      # Entry point
│   └── vite.config.ts
├── output/               # CSV файлы и билд фронтенда
├── config.json           # Конфигурация
├── proxy.txt             # Прокси (опционально)
└── package.json
```

## Использование

### Веб-интерфейс

1. **Сортировка**: клик на заголовок колонки
2. **Фильтры**: 
   - Числовые колонки: кнопка 🔍 → выбор оператора (>, <, >=, <=) и значение
   - Spot: фильтр по ✅/❌/Все
3. **Избранное**: клик на звезду ⭐
4. **Копирование контракта**: кнопка ⧉ рядом с адресом
5. **Ссылки**: CG, CR, CMC для просмотра на других платформах

### CSV экспорт

Файлы сохраняются в `output/`:
- `perp_screener_YYYY-MM-DD_HH-MM-SS.csv` — каждый запуск
- `perp_screener_latest.csv` — симлинк/копия последнего файла

## Troubleshooting

### Ошибка "429 Too Many Requests"

- Используйте прокси (`proxy.txt`)
- Уменьшите `concurrency` в `config.json`

### "Cannot find module 'express'"

```bash
npm install
```

### Фронтенд не обновляется

- Проверьте, что `perp_screener_latest.csv` обновляется
- Откройте консоль браузера (F12) для ошибок
- Очистите кеш браузера (Ctrl+Shift+R)

### Нет данных с CoinGecko

- Проверьте интернет-соединение
- Некоторые токены могут отсутствовать в CoinGecko
- Проверьте логи на ошибки API

## Changelog

### 2025-11-05
- ✨ Добавлен веб-интерфейс с React + TanStack Table
- 🎨 Темная тема с фиолетовыми акцентами
- 🔍 Поповер-фильтры для числовых колонок и spot
- ⭐ Плавная анимация избранного
- 📋 Копирование адресов контрактов
- 🔄 Автообновление каждые 60 сек
- 📊 Сортировка по 24h change
- 🆕 Подсветка токенов <10 дней
- 🚀 Production деплой с Express сервером
- 🔗 Прокси-поддержка для CoinGecko

## Лицензия

MIT
