# 🚀 Binance Perpetual Screener

Скринер перпетуальных контрактов Binance с данными CoinGecko и веб-интерфейсом для анализа.

## Возможности

- ✅ Парсинг всех USDT-M перпетуалов с Binance
- 📊 Получение данных с CoinGecko (market cap, FDV, onboard date, контракты)
- 💹 Отображение 24h изменений цены
- 🔄 **Автоматическое обновление данных на бэкенде (каждые 5 минут по умолчанию)**
- 🔍 Фильтры по числовым значениям и наличию спота
- ⭐ Избранные монеты с сохранением в localStorage
- 🆕 Подсветка новых токенов (<10 дней)
- 📋 Копирование адресов контрактов
- 🔗 Ссылки на CoinGecko, CryptoRank, CoinMarketCap
- 🔄 Автообновление данных на фронтенде каждые 60 секунд


## Установка

```bash
# Корневая папка
npm install

# Фронтенд
cd frontend
npm install
cd ..
```

## Конфигурация

Скопируйте `config.example.json` в `config.json` и настройте параметры:

```json
{
  "concurrency": 4,
  "priceTolerance": 0.2,
  "progressIntervalMs": 5000,
  "updateIntervalMs": 300000,
  "chainsPriority": ["ethereum", "binance-smart-chain", "..."],
  "filters": {
    "minMarketCapUsd": null,
    "maxMarketCapUsd": null,
    "minFdvUsd": null,
    "maxFdvUsd": null,
    "minOnboardDays": 0,
    "maxOnboardDays": null
  },
  "outputDir": "output"
}
```

### Параметры конфигурации

- **concurrency** (по умолчанию: 4): количество одновременных запросов к CoinGecko
- **priceTolerance** (по умолчанию: 0.2): максимальное отклонение цены между Binance и CoinGecko (20%)
- **progressIntervalMs** (по умолчанию: 5000): интервал вывода прогресса в лог (миллисекунды)
- **updateIntervalMs** (по умолчанию: 300000): интервал автообновления данных на сервере (5 минут)
- **chainsPriority**: приоритет блокчейнов для выбора контракта
- **filters**: фильтры для монет (market cap, FDV, дни с листинга)
- **outputDir**: папка для сохранения CSV файлов

**Примечание**: Параметр `updateIntervalMs` можно переопределить через переменную окружения `UPDATE_INTERVAL_MS`.


### Прокси (опционально)

Создайте `proxy.txt` со списком прокси (по одному на строку):

```
http://user:pass@host:port
socks5://host:port
```

## Разработка

### Запуск в режиме разработки

```bash
# Бэкенд + фронтенд одновременно
npm run dev:all
```

- Бэкенд: запускает скринер, генерирует CSV в `output/`
- Фронтенд: http://localhost:3000 с hot reload

### Только бэкенд

```bash
npm run start
```

### Только фронтенд

```bash
cd frontend
npm run dev
```

## Production деплой

### 1. Билд проекта

```bash
npm run build:all
```

Это:
- Создаёт `dist/` с скомпилированным TypeScript
- Билдит фронтенд в `output/dist/`

### 2. Запуск в production

**Новый подход (рекомендуется):**

Сервер теперь автоматически обновляет данные монет периодически:

```bash
npm run start:prod
```

Приложение:
- Запустится на http://localhost:8080
- Автоматически соберет данные при старте
- Будет обновлять данные каждые 5 минут (настраивается через `updateIntervalMs` в `config.json`)
- Отдаст веб-интерфейс и актуальные CSV файлы

**Настройка интервала обновления:**

Через конфигурацию:
```json
{
  "updateIntervalMs": 300000  // 5 минут в миллисекундах
}
```

Или через переменную окружения:
```bash
UPDATE_INTERVAL_MS=600000 npm run start:prod  # 10 минут
```

**Старый подход (ручное обновление):**

Если нужно запускать сбор данных вручную:

```bash
# Только сбор данных (без запуска сервера)
npm run start

# Запуск только веб-сервера (без автообновления)
npm run start:server
```

### 3. Деплой на сервер

#### Вариант A: PM2 (рекомендуется)

```bash
npm install -g pm2

# Запускаем сервер с автообновлением
pm2 start dist/server.js --name coincap-screener

pm2 save
pm2 startup
```

Теперь **не нужен** cron — сервер сам обновляет данные!

#### Вариант B: Docker

Создайте `Dockerfile`:

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --production
COPY . .
RUN npm run build:all
EXPOSE 8080
ENV UPDATE_INTERVAL_MS=300000
CMD ["node", "dist/server.js"]
```

```bash
docker build -t coincap-screener .
docker run -d -p 8080:8080 -e UPDATE_INTERVAL_MS=600000 coincap-screener
```

#### Вариант C: VPS/Dedicated

1. Установите Node.js 18+
2. Клонируйте репозиторий
3. `npm install && cd frontend && npm install && cd ..`
4. `npm run build:all`
5. Настройте systemd для автозапуска:

**systemd service** (`/etc/systemd/system/coincap-screener.service`):

```ini
[Unit]
Description=Binance Perpetual Screener
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/path/to/coincap_screener
ExecStart=/usr/bin/node dist/server.js
Restart=on-failure
Environment=NODE_ENV=production
Environment=UPDATE_INTERVAL_MS=300000

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable coincap-screener
sudo systemctl start coincap-screener
sudo systemctl status coincap-screener
```


## Структура проекта

```
├── src/
│   ├── index.ts          # Основной скринер
│   ├── server.ts         # Express сервер для production
│   ├── binance.ts        # API Binance
│   ├── coingecko.ts      # API CoinGecko
│   ├── config.ts         # Конфигурация
│   ├── match.ts          # Утилиты
│   └── utils/http.ts     # HTTP клиент с retry
├── frontend/
│   ├── src/
│   │   ├── App.tsx       # Основной компонент
│   │   ├── App.css       # Стили
│   │   ├── types.ts      # TypeScript типы
│   │   └── main.tsx      # Entry point
│   └── vite.config.ts
├── output/               # CSV файлы и билд фронтенда
├── config.json           # Конфигурация
├── proxy.txt             # Прокси (опционально)
└── package.json
```

## Использование

### Веб-интерфейс

1. **Сортировка**: клик на заголовок колонки
2. **Фильтры**: 
   - Числовые колонки: кнопка 🔍 → выбор оператора (>, <, >=, <=) и значение
   - Spot: фильтр по ✅/❌/Все
3. **Избранное**: клик на звезду ⭐
4. **Копирование контракта**: кнопка ⧉ рядом с адресом
5. **Ссылки**: CG, CR, CMC для просмотра на других платформах

### CSV экспорт

Файлы сохраняются в `output/`:
- `perp_screener_YYYY-MM-DD_HH-MM-SS.csv` — каждый запуск
- `perp_screener_latest.csv` — симлинк/копия последнего файла

## Troubleshooting

### Ошибка "429 Too Many Requests"

- Используйте прокси (`proxy.txt`)
- Уменьшите `concurrency` в `config.json`

### "Cannot find module 'express'"

```bash
npm install
```

### Фронтенд не обновляется

- Проверьте, что `perp_screener_latest.csv` обновляется
- Откройте консоль браузера (F12) для ошибок
- Очистите кеш браузера (Ctrl+Shift+R)

### Нет данных с CoinGecko

- Проверьте интернет-соединение
- Некоторые токены могут отсутствовать в CoinGecko
- Проверьте логи на ошибки API

## Changelog

### 2025-11-19
- 🔄 **Автоматическое обновление данных на бэкенде**
  - Сервер теперь периодически обновляет данные монет (каждые 5 минут по умолчанию)
  - Настраивается через `updateIntervalMs` в config.json или переменную окружения `UPDATE_INTERVAL_MS`
  - Первоначальное обновление данных при старте сервера
  - API endpoint `/api/latest-data` для проверки последнего обновления
  - API endpoint `/api/refresh` для принудительного обновления данных
  - Graceful shutdown с корректной остановкой интервалов
- 📝 Обновлена документация с примерами деплоя

### 2025-11-05
- ✨ Добавлен веб-интерфейс с React + TanStack Table
- 🎨 Темная тема с фиолетовыми акцентами
- 🔍 Поповер-фильтры для числовых колонок и spot
- ⭐ Плавная анимация избранного
- 📋 Копирование адресов контрактов
- 🔄 Автообновление каждые 60 сек
- 📊 Сортировка по 24h change
- 🆕 Подсветка токенов <10 дней
- 🚀 Production деплой с Express сервером
- 🔗 Прокси-поддержка для CoinGecko


## Лицензия

MIT
