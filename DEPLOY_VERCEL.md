# 🚀 Деплой на Vercel

## ⚠️ Важно: Ограничения Vercel

Vercel — это serverless платформа, которая **не подходит** для полного деплоя этого проекта из-за:

1. **Read-only файловая система** — нельзя сохранять CSV файлы
2. **Нет cron jobs** — нельзя запускать скринер периодически
3. **Timeout 10-60 секунд** — скринер работает дольше

## 🎯 Рекомендуемые решения

### Вариант 1: Vercel (фронтенд) + Railway/Render (бэкенд)

**Лучший вариант для production**

#### Шаг 1: Деплой бэкенда на Railway/Render

**Railway (рекомендую):**

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите
railway login

# Создайте проект
railway init

# Добавьте переменные окружения
railway variables set PORT=8080

# Деплой
railway up
```

**Или Render:**
1. Создайте новый Web Service на render.com
2. Подключите GitHub репозиторий
3. Build Command: `npm install && npm run build`
4. Start Command: `node dist/index.js && node dist/server.js`

#### Шаг 2: Настройте Cron для обновления данных

**Railway:**
```bash
# Создайте отдельный Cron Job
railway cron add "0 * * * *" "node dist/index.js"
```

**Render:**
1. Создайте Cron Job
2. Command: `node dist/index.js`
3. Schedule: `0 * * * *` (каждый час)

#### Шаг 3: Деплой фронтенда на Vercel

```bash
# Установите Vercel CLI
npm install -g vercel

# Перейдите в папку фронтенда
cd frontend

# Деплой
vercel

# Production деплой
vercel --prod
```

Укажите переменную окружения в Vercel:
- `VITE_API_URL` = URL вашего бэкенда на Railway/Render

### Вариант 2: Полный деплой на одной платформе

#### Railway (все в одном)

```bash
railway init
railway up
```

Добавьте в `railway.json`:
```json
{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build:all"
  },
  "deploy": {
    "startCommand": "node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

#### Render (все в одном)

1. Создайте Web Service
2. Build: `npm install && npm run build:all`
3. Start: `node dist/server.js`
4. Добавьте Cron Job для `node dist/index.js`

### Вариант 3: VPS/Dedicated Server (полный контроль)

**DigitalOcean, Linode, Vultr, Hetzner:**

```bash
# На сервере
git clone <your-repo>
cd coincap_screener

# Установка
npm install
cd frontend && npm install && cd ..

# Билд
npm run build:all

# PM2 для автозапуска
npm install -g pm2

# Скринер каждый час
pm2 start dist/index.js --name screener --cron "0 * * * *" --no-autorestart

# Веб-сервер
pm2 start dist/server.js --name web

pm2 save
pm2 startup
```

### Вариант 4: Cloudflare Pages + Workers

**Фронтенд:** Cloudflare Pages  
**Бэкенд:** Cloudflare Workers + D1/R2 для хранения

Требует переписывания бэкенда под Workers API.

## 📊 Сравнение платформ

| Платформа | Цена | Сложность | Cron | Файлы | Рекомендация |
|-----------|------|-----------|------|-------|--------------|
| **Railway** | $5+/мес | Низкая | ✅ | ✅ | ⭐⭐⭐⭐⭐ Лучший |
| **Render** | $7+/мес | Низкая | ✅ | ✅ | ⭐⭐⭐⭐ Хороший |
| **VPS** | $5+/мес | Средняя | ✅ | ✅ | ⭐⭐⭐⭐ Полный контроль |
| **Vercel** | Free | Низкая | ❌ | ❌ | ⭐⭐ Только фронтенд |
| **Heroku** | $7+/мес | Низкая | ✅ | ❌ | ⭐⭐⭐ Нужно S3 |

## 🎯 Быстрый старт (Railway - рекомендую)

```bash
# 1. Установите Railway CLI
npm install -g @railway/cli

# 2. Войдите
railway login

# 3. Инициализируйте проект
railway init

# 4. Деплой
railway up

# 5. Откройте в браузере
railway open
```

Railway автоматически:
- Обнаружит Node.js проект
- Установит зависимости
- Соберёт проект
- Запустит сервер
- Даст вам URL

## 🔧 Настройка для Railway/Render

Обновите `frontend/src/App.tsx`:

```typescript
const API_URL = import.meta.env.VITE_API_URL || '';

const loadData = async () => {
  try {
    const res = await fetch(`${API_URL}/output/perp_screener_latest.csv`);
    // ...
  }
};
```

Добавьте `frontend/.env.production`:
```
VITE_API_URL=https://your-app.railway.app
```

## 📝 Итого

**Рекомендую:**
1. **Railway** — самый простой и быстрый способ
2. **Render** — если нужен бесплатный tier (с ограничениями)
3. **VPS** — для полного контроля и низкой цены

**Не рекомендую Vercel** для этого проекта, так как нужен постоянно работающий бэкенд с файловой системой и cron jobs.

