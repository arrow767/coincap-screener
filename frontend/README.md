# Binance Perpetual Screener Frontend

Топовый фронтенд для просмотра данных перпетуалов Binance с CoinGecko.

## Особенности

- ✨ Современный градиентный дизайн
- 📊 Мощная таблица с сортировкой (TanStack Table)
- ⭐ Избранное с сохранением в localStorage
- 🆕 Подсветка новых токенов (зелёная)
- 📈 Прогресс-бар Locked % (market cap / FDV)
- 🔗 Быстрые ссылки на CoinGecko, CryptoRank, CoinMarketCap
- 🔄 Автообновление каждую минуту
- ✅/❌ Эмодзи для has_spot_usdt
- 🎨 Анимации и hover-эффекты

## Установка

```bash
cd frontend
npm install
```

## Запуск

```bash
npm run dev
```

Откроется `http://localhost:3000`

## Сборка

```bash
npm run build
```

## Структура полей

- **Symbol** — тикер на Binance
- **Price** — цена перпетуала в USD
- **Change 24h** — изменение за 24 ч (заглушка, можно добавить API)
- **Days** — дней на Binance perpetual (perp_onboard_days)
- **Spot** — ✅ если есть спот USDT, ❌ если нет
- **Market Cap** — капитализация в M
- **FDV** — полная оценка в M
- **Locked %** — прогресс-бар mcap/fdv
- **Chain** — блокчейн контракта
- **Contract** — адрес контракта (сокращённо)
- **Links** — CG, CR, CMC

## CSV-файл

Фронтенд ищет `/output/perp_screener_latest.csv`. Создайте симлинк или скопируйте последний CSV туда.

Windows:
```powershell
cd frontend/public
mkdir output
copy ../../output/perp_screener_*.csv output/perp_screener_latest.csv
```

Linux/WSL:
```bash
cd frontend/public
mkdir output
ln -s ../../output/$(ls -t ../../output/perp_screener_*.csv | head -1) output/perp_screener_latest.csv
```

## Избранное и новые токены

- Избранное сохраняется в `localStorage` (`favorites`)
- Новые токены подсвечиваются зелёным с анимацией
- Список новых сохраняется в `localStorage` (`newCoins`)

